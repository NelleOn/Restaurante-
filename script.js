// Select the booking form
const form = document.getElementById("bookingForm");

//only resets when coming back from fromsprees thank you page
window.addEventListener("pageshow", (event) => {
    const if 